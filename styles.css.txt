body {
    margin: 0;
    padding: 0;
    background: url('texture1.jpg') no-repeat center center fixed;
    background-size: cover;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
}

.container {
    text-align: center;
    max-width: 90%;
}

.phrase {
    font-family: 'Courier New', Courier, monospace;
    font-size: 50px;
    color: white;
    margin-bottom: 20px;
}

.countdown {
    font-family: 'Arial', sans-serif;
    font-size: 30px;
    color: white;
    margin-bottom: 30px;
    padding: 15px;
    background-color: rgba(0, 0, 0, 0.7);
    border-radius: 15px;
    display: inline-block;
}

.countdown span {
    display: inline-block;
    margin: 0 5px;
}

.countdown .label {
    display: block;
    font-size: 15px;
    margin-top: 5px;
}

.buttons {
    display: flex;
    justify-content: center;
    gap: 10px;
}

.button {
    display: inline-block;
    padding: 8px 15px;
    font-family: 'Arial', sans-serif;
    font-size: 14px;
    color: white;
    background-color: #333;
    border: none;
    border-radius: 20px;
    text-decoration: none; /* Evita que parezca un link */
    transition: background-color 0.3s, transform 0.3s;
}

.button:hover {
    background-color: #555;
    transform: scale(1.05);
}

/* Evitar que los links sean subrayados en móviles */
.button:link, .button:visited {
    text-decoration: none;
}

/* Media Queries para dispositivos móviles */
@media (max-width: 768px) {
    .phrase {
        font-size: 40px;
    }

    .countdown {
        font-size: 25px;
        padding: 10px;
    }

    .button {
        font-size: 12px;
        padding: 6px 10px;
    }
}

@media (max-width: 480px) {
    .phrase {
        font-size: 30px;
    }

    .countdown {
        font-size: 20px;
        padding: 8px;
    }

    .button {
        font-size: 10px;
        padding: 5px 8px;
    }
}
